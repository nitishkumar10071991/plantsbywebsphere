# Generated by IBM TransformationAdvisor
# Mon Dec 03 21:17:50 UTC 2018

apiVersion: v1
kind: Service
metadata:
  name: plantsbywebsphere
  labels:
    app: plantsbywebsphere
spec:
  ports:
    - port: 9080
  selector:
    app: plantsbywebsphere
  type: NodePort

---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  labels:
    app: plantsbywebsphere
  name: plantsbywebsphere
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: plantsbywebsphere
    spec:
      containers:
      - image: mycluster.icp:8500/default/plantsbywebsphere:latest
        name: plantsbywebsphere
        env:
          - name: WLP_LOGGING_CONSOLE_FORMAT
            value: json
          - name: WLP_LOGGING_CONSOLE_LOGLEVEL
            value: info
          - name: WLP_LOGGING_CONSOLE_SOURCE
            value: message,trace,accessLog,ffdc
        ports:
        - containerPort: 9080
          protocol: TCP
